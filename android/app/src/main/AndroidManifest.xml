<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            AdMob app id. Public value (it ships inside the APK); the ids live in
            docs/store/ids.md and are mirrored in src/platform/adUnits.ts. The Google
            Mobile Ads SDK crashes at startup if this meta-data is missing.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5130289288594607~4830589570"
        />

        <!--
            Play Games Services project id (docs/store/ids.md). The leading "\ " keeps the
            numeric value a string, which the Play Games SDK requires. The same id unlocks
            cloud save (CloudSavePlugin.java) once Saved Games is on for the project.
        -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="\ 1035420780939"
        />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Advertising ID, required by AdMob on Android 13+ and declared in Play Data safety. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
</manifest>
